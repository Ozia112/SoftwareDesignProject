# Copia este archivo a demo/.env.demo y rellena los valores.
# demo/.env.demo está en .gitignore — nunca se sube al repo.

CLAUDE_API_KEY=sk-ant-pon-tu-key-aqui

# Opcionales — por defecto apuntan a los contenedores Docker del demo
# DATABASE_URL=postgresql://app:app@localhost:5432/saas_dev
# ORCHESTRATOR_URL=http://localhost:3000
# TENANT_ID=demo-tenant
